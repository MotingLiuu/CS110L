# 编译生成的产物目录（包含可执行文件、依赖包的编译结果等）
# 这是最核心需要忽略的目录，它体积巨大且完全由本地编译生成
/target/

# 如果你使用的是 Cargo 的某些扩展工具（如 cargo-clippy 或旧版生成的文件）
# 可能会产生一些特定的历史或临时文件，通常已被 target/ 覆盖，但保留作为保险
**/*.rs.bk

# 仅当你开发的是「终端应用/服务（Binary）」时，不要忽略 Cargo.lock。
# 仅当你开发的是「供他人调用的库（Library）」时，可以选择忽略 Cargo.lock（不过现在更多人倾向于保留它）。
# 如果你确定要忽略它，可以取消下面这行的注释：
# Cargo.lock

# IDE 和编辑器生成的专属配置文件（可选，建议添加到全局 gitignore 中，但也常写在这里）
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp